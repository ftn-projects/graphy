[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "graphy-rdf-data-source"
version = "0.1"

[tool.setuptools.packages.find]
where = ["src"]
namespaces = false